// ESLint 8 classic config. `npm run lint` has been in package.json since the
// project was scaffolded but no config file was ever committed, so the script
// always exited 2 with "couldn't find a configuration file" -- and CI only ran
// `npm run build`, so nothing noticed.
//
// Rule set is the stock Vite React-TS template, matching the plugins already in
// devDependencies (@typescript-eslint 6, react-hooks 4, react-refresh 0.4).
module.exports = {
  root: true,
  env: { browser: true, es2020: true },
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:react-hooks/recommended',
  ],
  ignorePatterns: ['dist', 'node_modules', '.eslintrc.cjs', 'playwright-report', 'test-results'],
  parser: '@typescript-eslint/parser',
  parserOptions: {
    ecmaVersion: 'latest',
    sourceType: 'module',
  },
  plugins: ['react-refresh'],
  rules: {
    'react-refresh/only-export-components': ['warn', { allowConstantExport: true }],
  },
  overrides: [
    {
      // Playwright specs run in Node, not the browser.
      files: ['tests/**/*.ts', '*.config.ts'],
      env: { browser: false, node: true },
    },
    {
      // Generated by the shadcn/ui CLI (see components.json). Upstream ships a
      // component and its cva variants from one file; reshaping vendored files
      // to satisfy a fast-refresh hint would only make future regeneration
      // conflict.
      files: ['src/components/ui/**'],
      rules: { 'react-refresh/only-export-components': 'off' },
    },
  ],
}
