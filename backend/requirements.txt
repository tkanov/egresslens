# Bounded rather than exact-pinned: a lower bound at versions this code is known
# to work against, and an upper bound at the next major so an upstream breaking
# release cannot fail `main` with no change in this repo. These were seven bare
# package names against a 3.9-3.12 CI matrix.
#
# Bump deliberately; Dependabot opens the PRs (see .github/dependabot.yml).
fastapi>=0.141.1,<1.0
uvicorn>=0.52.0,<1.0
sqlalchemy>=2.0.51,<3.0
pydantic>=2.13.4,<3.0
python-multipart>=0.0.32,<0.1
pyyaml>=6.0.3,<7.0

# Required at runtime by fastapi.testclient, which the backend tests use.
httpx>=0.28.1,<1.0

# Not listed above, but required: the `egresslens` CLI package, which owns the
# policy and enrichment engine that app.policy and app.enrichment re-export.
# Install it separately, from the repo root:
#
#     pip install -e ./cli
#
# It is a comment rather than a requirement line because pip resolves relative
# paths in a requirements file against the current working directory, not the
# file: the docs run `pip install -r requirements.txt` from inside backend/ and
# CI runs `pip install -r backend/requirements.txt` from the repo root, and no
# single relative path satisfies both.
